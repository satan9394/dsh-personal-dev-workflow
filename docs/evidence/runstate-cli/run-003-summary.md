# Run #3 总结 + 三轮实测复盘

- 日期：2026-09-12
- Mission：加固 runstate 的预算守卫并补最小自动化测试
- 结果：**Mission 完成**（DoD 五条全达成）→ 停止条件第 1 条（DoD met）；预算同时用尽（2/2）
- 恢复方式：新 Mission 从 `Deferred Backlog` **按相关性采纳** 2 项为 T004 / T005（不是"发现自动变任务"）

## 本轮卡片

| 卡 | 内容 | 来源 | 执行器 | 验收 |
|---|---|---|---|---|
| T004 | 非法预算行必须报错（原先静默跳过） | 采纳自 audit backlog | 子代理 5466f071 | PASS → 自动合入 |
| T005 | `tests/run-tests.mjs`（9 条黑盒用例）+ README 已知限制更新 | 采纳自 audit backlog | 子代理 37d1ff94 | PASS → 自动合入 |

## 指挥侧验证证据（不采信执行器报告）

- T004：非法行 `abc / 2` → `check` exit 1 并报「Budget 第 26 行格式非法」；`advance` exit 1 且**哈希前后一致**；上限非法 `1 / xyz` → exit 1；Budget 内散文行 → exit 1；无上限行仍可推进；**真实状态文件回归 exit 0**
- T005：`node tests/run-tests.mjs` **9/9 全绿 exit 0**；`RS_TEST_FORCE_FAIL=1` → **8/9 exit 1**；`README.md` 已知限制已改口径；五处哈希与报告一致；临时目录残留 0
- 附加收益：执行器发现旧解析器在 `1 / xyz` 时会把上限静默丢弃（写成 `2 / xyz`），守卫彻底消失——比卡面描述更严重，现已拒绝

## 三轮实测（Run #1 → #3）：控制器到底证明了什么

| 机制 | 实测结果 |
|---|---|
| DoD 先行 | 每轮开跑前写 Mission / DoD / Out of scope；本轮 DoD 五条全部以可执行证据达成 |
| 有限 WorkSet | 每轮物化 2–3 张卡（≤8），每张都带 `来源`（planned / blocker / human-requested） |
| 自动验收 | **5 张卡全部自动合入**，无一需要人工逐卡签核；人工只处理例外（三轮均无例外触发） |
| 机械预算守卫 | 越界递增被拒绝且**文件字节不变**（多次哈希比对一致） |
| 停止条件 #2（预算耗尽） | Run #1 在完成卡数 2/2 时停机，T003 未执行 |
| 停止条件 #1（DoD 达成） | Run #2、Run #3 均以 DoD 达成为正常收尾 |
| 审计无执行权 | 5 条发现只进 `Deferred Backlog`，未被顺手修改、未自动变卡 |
| backlog → Mission 采纳 | Run #3 主动从 backlog 挑 2 条立为卡（与"发现自动变队列"相反） |
| 文件交接 / 恢复 | Run #2 仅凭 `RUN_STATE.md` 接手，未重读任何历史 |

## 复盘沉淀（已写进项目 AGENTS.md）

1. **别这样做：Budget 节里不要写散文行** —— 任何 `- 标签: 值` 行都会被当作计数行（起因：`abc / 2` 笔误曾让守卫静默失效）。
2. **别这样做：验收标准不要写死现场快照值**（如 `1/2`），要写相对规则（Run #1 已踩，卡面与模板默认冲突）。

## 需用户裁决 / 继续留在 backlog

- **（rule）** 测试脚本用 `fs.rmSync` 清理自建临时目录（Node 零依赖无回收站 API），作用域仅限自建 `runstate-tests-*` 且有双重前缀守卫——与"删除进回收站"铁律存在张力。
- （audit）缺冒号笔误 `- 完成卡数 3 / 2` 不算计数行，`check` 仍放行。
- （audit）`Worker 数: 2（上限 3）` 中"上限 3"只是备注、非硬上限。
- （audit）测试仅黑盒冒烟，无单测与覆盖率。
- （audit）0904 工作区 skill 副本为旧版 v0.3，与 canonical v0.4.0 不一致。
- （audit）`advance` 无 `--dry-run`。
